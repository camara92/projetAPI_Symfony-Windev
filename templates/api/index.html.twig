{% extends 'base.html.twig' %}

{% block title %}
	Liste de regions
{% endblock %}

{% block body %}


	<div class="container my-5">
    <h1 class="text-bold text-center">Liste des regions en France 
    </h1>
		<div class="row">
				{% for maRegion  in mesregions %}
			<div class="col-md-3  ">
                <div class="card border-primary mb-3" style="max-width: 20rem;">
					<div class="card-header"> {{ maRegion.code }} </div>
					<div class="card-body">
						<h4 class="card-title">{{ maRegion.nom }} </h4>
						{# <p class="card-text"> Daouda CAMARA </p> #}
					</div>
				</div>
			</div>
                {% endfor %}
				
		</div>

        <a href="{{path("listeDepsParRegions")}}" class="btn btn-primary">Afficher region par département </a>
	</div>
{% endblock %}
